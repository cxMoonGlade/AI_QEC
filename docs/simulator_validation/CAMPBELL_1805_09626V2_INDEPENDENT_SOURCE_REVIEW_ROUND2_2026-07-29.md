# Campbell et al. 1805.09626v2 — independent source-only review, Round 2

Review date: 2026-07-29  
Review scope: fixed PDF plus the repaired candidate source note and memory-depth audit  
Verdict: **PASS**

This review independently reopened the fixed source and did not accept the audit's project
statements as source evidence. The repaired candidate note and audit are source-faithful at their
declared strengths. No required semantic, locator, relation, replay, or hash repair remains.

## Fixed reviewed objects

| object | SHA-256 | independent check |
|---|---|---|
| `docs/papers/1805.09626v2.pdf` | `619f3a5fe047481ef1fc434255e63e0ca3428ca594805a34d9897ec0e9fb4fd5` | Valid unencrypted PDF 1.5, 1,748,981 bytes, 11 pages; title page and arXiv margin identify `1805.09626v2`. |
| `docs/papers/reading_notes/campbell_markovian_embedding_1805.09626v2_source_review.md` | `60a007a9fb86017f370a772d15739f15552f06ef1189c27084600995019d7789` | Repaired source-only candidate; current admission fields remain pending. |
| `docs/simulator_validation/CAMPBELL_1805_09626V2_MEMORY_DEPTH_AUDIT_2026-07-29.md` | `364ca4438a1d8ddabb06c87cf54e36499cc93c3414afdc11a438a7b3016e1916` | Repaired separate audit packet. |

The note's `source_sha256` and `audit_packet_sha256` exactly match the first and third rows. The
source identity now stays within the fixed object: eleven-page arXiv v2 preprint, with no unsupported
journal-publication claim.

The full eleven-page PDF was traversed again. Rendered pages 1–3, 5–7, and 9 were visually
rechecked for the version line, Eqs. (1)–(5), Eqs. (7)–(18), the fixed-memory proof, the scheduling
qualification, and the generality boundary. The note's `visually_checked_pages` names exactly these
pages.

## Independent source reconstruction

| source chain | exact source location | source-supported strength |
|---|---|---|
| Factorized input and pair collisions | Eq. (1), Eq. (2), PDF p. 2 | General density-operator factors; pairwise unitary collisions generated by the printed anisotropic Pauli Hamiltonian. |
| Collision blocks | Eqs. (3)–(4) and following paragraph, PDF p. 2 | Exact \(S\)-collision and AA index blocks; \(l<m\); arbitrary ordering of pairwise AA interactions. |
| Memory endpoints | paragraph after Eq. (4), PDF pp. 2–3 | No AA collisions gives the fresh-ancilla memoryless model; perfect AA SWAPs make \(S\) behave as if it repeatedly meets the same ancilla. |
| BLP diagnostic | Eq. (5) and discussion, PDF p. 3 | A trace-distance revival for at least one initial pair is sufficient; monotonic decay for one selected pair is inconclusive. |
| \(d=1\) advancing enlarged state | Eqs. (7)–(10), PDF p. 5 | CPTP composition on \(S+E_n\), with the retained ancilla identity advancing with \(n\); tracing \(E_n\) yields the generally non-Markovian reduced map. |
| Explicit \(d=2\) advancing enlarged state | unnumbered \(\Phi_n^{(2)}\) maps, Eq. (11), and following reduced-map equation, PDF p. 5 | Explicit CPTP composition on \(S\) plus two advancing ancillas. |
| Arbitrary-\(d\) enlarged-state statement | paragraph following Eq. (11), PDF p. 5 | A source-stated natural extension, not a printed general \(\Phi^{(d)}\) or \(\Lambda^{(d)}\) equation. |
| Fixed-memory \(d=1\) equivalence | Eqs. (12), (14)–(17), PDF pp. 5–7 | SWAP identities, index relabelling, fresh ancillas, and partial traces produce the same reduced dynamics with fixed memory \(E_1\). |
| Fixed-memory \(d=2\) equivalence | Eq. (13), Eq. (18), and following paragraph, PDF p. 7 | The source states equivalence for two fixed memory ancillas. |
| Arbitrary-\(d\) fixed-memory statement | paragraph following Eq. (18), PDF p. 7 | A source-stated analogous approach, not a separately printed general proof or equation. |
| Scheduling boundary | final paragraph of Sec. V, PDF p. 7 | Same-fixed-ancilla SWAP replay requires all \(S\)-memory interactions before intra-environment collisions; another interleaving retains last-\(d\) relevance but not the same-fixed-ancilla guarantee. |
| Generality boundary | final two paragraphs of Sec. VII, PDF p. 9 | The source does not establish that arbitrary non-Markovian dynamics admit this collision-model construction. |

## Prior-repair closure

| required repair from Round 1 | Round-2 finding |
|---|---|
| Remove unsupported journal metadata. | **Closed.** The note now uses `publication_status = "preprint"` and cites only the title page and arXiv version line. |
| Make facts and tensor-network gaps atomic. | **Closed.** Memory-depth definition and embedding size are separate; \(d=2\) and arbitrary-\(d\) statements are separate; the five source-local gaps are five records with distinct Fact IDs. |
| Preserve perfect-AA-SWAP and Eq. (3)–(4) index/order semantics. | **Closed.** The note and audit explicitly say AA SWAP, give both printed block bounds and \(l<m\), and preserve arbitrary AA pair ordering. |
| Separate \(d=1\), explicit \(d=2\), and source-stated arbitrary \(d\). | **Closed.** Both the evidence records and closure table preserve these different evidentiary strengths for advancing and fixed-memory descriptions. |
| Replay Eqs. (14)–(18), not only Eqs. (7)–(13). | **Closed.** The audit reconstructs the nested stream, SWAP identities, index movement, partial-trace movement, \(d=1\) equality, the stated \(d=2\) equality, and the arbitrary-\(d\) analogue. |
| Add the observable and failure-regime replay. | **Closed.** The audit evolves two initial states under one common dynamics, computes Eq. (5), and preserves the sufficient-positive/inconclusive-negative distinction. |
| Preserve fresh-ancilla and scheduling requirements. | **Closed.** They appear in the note, assigned rows, operation replay, project boundary, and kill conditions. |
| Do not equate the project ladder with the paper's embedding or complexity taxonomy. | **Closed.** The audit explicitly says the closed \(2\times w\) ladder neither reproduces the advancing collision stream nor implements Eqs. (14)–(18), and labels its coupling levels as project-defined. |
| Recompute and bind exact hashes. | **Closed.** The source and audit digests in note metadata equal the independently computed files. |

## Fact, gap, and relation audit

All 21 `paper_fact` records are source-only, independently locatable, and atomic at the level needed
for retrieval. Supporting prose under a record only qualifies that record; it does not add a
project bridge. The five `literature_gap` records each state one source-local absence and use the
explicit full scientific-body search boundary `PDF pp. 1–9`.

The three relations have existing `paper_fact` endpoints:

- `collision-memory-depth` points to the atomic memory-depth definition;
- `xyz-collision-hamiltonian` points to the collision-Hamiltonian definition;
- `finite-memory-markovian-embedding` points to the \(d=1\) embedding fact.

Each predicate is compatible with its evidence record, and each `object_label` occurs in the
referenced Claim. There are no dangling or gap-targeting relations.

## Operation-replay judgment

The replay now closes the complete source-local chain without silently promoting an analogy:

1. factorized input \(\rightarrow\) exact collision Hamiltonian and block schedule;
2. separate \(d=1\) and \(d=2\) enlarged CPTP updates \(\rightarrow\) reduced maps;
3. source-stated arbitrary-\(d\) extension, explicitly labelled as an analogue;
4. common dynamics for two initial states \(\rightarrow\) Eq. (5) trace-distance witness and its
   negative-result limitation;
5. original nested stream \(\rightarrow\) SWAP identities \(\rightarrow\) fixed \(E_1\) plus fresh
   ancillas \(\rightarrow\) partial-trace reduction \(\rightarrow\) equality for \(d=1\);
6. stated \(d=2\) fixed-memory equality and stated arbitrary-\(d\) analogue;
7. whole-memory-before-AA timing gate and the counter-schedule that loses the fixed-register
   guarantee.

The audit does not invent a numbered arbitrary-\(d\) map, a general proof, or a project
implementation equivalence.

## Project boundary

The audit keeps project application outside the source-only note and marks the relevant
non-equivalences. In particular:

- Eq. (1) permits mixed factors; a pure-state carrier requires a separately frozen purity,
  purification, or trajectory assumption.
- A persistent finite register alone is not Campbell et al.'s fixed-memory construction.
- The proposed closed \(2\times w\) unitary ladder omits the paper's fresh-ancilla/SWAP/trace
  construction and does not claim its equality.
- The one-axis/two-axis/isotropic ladder is explicitly project-defined rather than source-defined
  “noise complexity.”
- PEPS layout, Bernoulli masking, bond caps, contraction behavior, runtime, and comparison design
  receive no guarantee from this source.

This review accepts only those boundary statements; it makes no finding that any project
implementation claim is true.

## Real parser shadow

The current note intentionally remains pending, so the production parser must reject it until an
independent reviewer sets the two admission fields. To test the post-review object without editing
the candidate, an isolated `/tmp` shadow copied the exact PDF, note, and audit and changed only:

```text
admission_status = "source_only_reviewed"
admission_reviewer = "campbell-round2-independent-source-review"
```

An exact diff confirmed those were the only note changes. The repository's actual command

```text
python tools/literature_rag.py --repo-root <shadow-root> audit \
  --notes-dir docs/papers/reading_notes --strict
```

returned `verification_mode = "artifact_verified"`, `candidate_count = 1`,
`validated_count = 1`, `excluded_count = 0`, and `paper_fact_count = 21`. This exercised the real
source-PDF hash, audit-packet hash, classified-section, visually-checked-page, relation-endpoint, and
source-only-prose checks. The admission-only shadow note hash was
`23f0dfb5efe0913f8fff5eb340895ec6a2c1960abddc83fd586c12cc098ba3c3`;
the candidate note itself remains unchanged at the hash recorded above.

## Admission decision

- independent full-text read: `complete`
- candidate evidence: `persisted`
- operation replay: `complete`
- assigned-row status: `closed` at the separately declared \(d=1\), explicit \(d=2\), and
  source-stated arbitrary-\(d\) strengths
- independent source-only verdict: **PASS**
- remaining action: the corpus owner may set the note's admission reviewer/status and then bind the
  resulting note hash in the explicit corpus manifest; this review neither edits nor admits it
