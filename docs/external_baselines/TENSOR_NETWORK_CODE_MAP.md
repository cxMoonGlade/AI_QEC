# External tensor-network implementation code map

<!-- external-tn-code-map-inputs-sha256: c7b83e60fc132caa95e0470a82dc0db71accdaf958fa800e5611246499bb6278 -->

Generated by `python tools/gen_external_tn_code_map.py`. The 7 upstream repositories
are pristine read-only inputs; adapters and comparison scripts belong in this repository.
This is a learning/navigation map, not scientific evidence or an oracle verdict.
The complete structural inventory of all external clones is
[`EXTERNAL_CODE_MAP.md`](EXTERNAL_CODE_MAP.md).

## Fast routing

| Question | First implementation | Second implementation route | Important boundary |
|---|---|---|---|
| Qubit circuit MPS gate/update/sampling | Qiskit Aer | ITensorMPS gate application | Aer is qubit-only |
| Canonical MPS truncation and discarded Schmidt weight | ITensorMPS | YASTN MPS | Do not transfer a 1D bound to PEPS |
| Finite PEPS gate evolution and local environment truncation | YASTN | exact d3 plus our carrier | YASTN diagnostics are not record-law certificates |
| Circuit-style finite PEPS complete-state execution | Quimb | Pepsy (Quimb-backed) | shared substrate means cross-route agreement is not library independence |
| Arbitrary-graph Julia tensor-network evolution | TensorNetworkQuantumSimulator.jl | Quimb/Pepsy | no admitted public d5 complete-state exporter |
| Boundary-MPS contraction | YASTN | exact finite contraction | Separate contraction error from state truncation |
| CTMRG and split-CTMRG | variPEPS | YASTN CTM | variPEPS is periodic iPEPS, not finite OBC trajectory |
| Full multi-round detector/observable record | none of these directly | our exact d3 record oracle | External state metrics require an explicit record bridge |

## Comparison architecture

```text
frozen circuit / gates / tensors / seeds
        |
        +-- Aer MPS -------- qubit circuit record/state comparator
        +-- ITensorMPS ----- canonical MPS/truncation comparator
        +-- YASTN ---------- finite MPS/PEPS evolution + environment comparator
        +-- Quimb ---------- finite PEPS circuit + complete-state comparator
        +-- Pepsy ---------- finite PEPS gate + complete-state comparator
        +-- TNQS.jl -------- arbitrary-graph evolution; no admitted d5 exporter
        `-- variPEPS ------- periodic CTMRG/contraction comparator
                         |
                 neutral artifact manifest
                         |
             repo-owned comparison and falsifier
```

## Qiskit Aer

- Clone: `external/baselines/qiskit-aer`
- Environment: `ecs-baseline-aer`
- Frozen input: `main` at `837c3ef3c39248aae936580360c22224dcefb265`
- Scope: Independent C++ qubit-MPS circuit executor and sampler; no PEPS or qutrit carrier.
- Data flow: `Aer method/config -> MPS state dispatcher -> local tensor updates/SVD -> measurement/sample -> saved MPS/results`

| Read in this order | Path | What to learn |
|---|---|---|
| Configuration and public controls | `external/baselines/qiskit-aer/src/framework/config.hpp` | truncation threshold, maximum bond, sampling algorithm, logging |
| Executor boundary | `external/baselines/qiskit-aer/src/simulators/matrix_product_state/matrix_product_state.hpp` | operation dispatch, measurement, save/set MPS |
| State and update core | `external/baselines/qiskit-aer/src/simulators/matrix_product_state/matrix_product_state_internal.hpp` | MPS ownership and gate/update algorithms |
| Update implementation | `external/baselines/qiskit-aer/src/simulators/matrix_product_state/matrix_product_state_internal.cpp` | concrete contractions, swaps, probabilities |
| Site tensor representation | `external/baselines/qiskit-aer/src/simulators/matrix_product_state/matrix_product_state_tensor.hpp` | left/right tensors and bond-local operations |
| SVD policy | `external/baselines/qiskit-aer/src/simulators/matrix_product_state/svd.hpp` | rank selection and truncation interface |
| SVD implementation | `external/baselines/qiskit-aer/src/simulators/matrix_product_state/svd.cpp` | discard logic and decomposition backend |
| Resource estimator | `external/baselines/qiskit-aer/src/simulators/matrix_product_state/matrix_product_state_size_estimator.hpp` | memory estimates, not accuracy evidence |
| Save/restore test | `external/baselines/qiskit-aer/test/terra/backends/aer_simulator/test_save_matrix_product_state.py` | external representation round trip |

Questions to carry while reading:

- How are threshold and max-bond combined when choosing a retained rank?
- Where are nonlocal gates converted into swaps, and does swap direction change truncation order?
- How do direct MPS measurement and probability-vector sampling differ?

## YASTN

- Clone: `external/baselines/yastn`
- Environment: `ecs-baseline-yastn`
- Frozen input: `master` at `595bd802ba0753a187b4bf7fd5c6d5007c0170d0`
- Scope: Independent Python tensor stack with OBC MPS plus finite/infinite PEPS, NTU, CTM, BP, and BoundaryMPS.
- Data flow: `geometry/state -> gates/evolution -> selected environment metric -> bond truncation -> measurement/sampling`

| Read in this order | Path | What to learn |
|---|---|---|
| MPS representation | `external/baselines/yastn/yastn/tn/mps/_mps_obc.py` | OBC MPS/MPO structure and canonical operations |
| MPS compression | `external/baselines/yastn/yastn/tn/mps/_compression.py` | variational compression and truncation |
| MPS environments | `external/baselines/yastn/yastn/tn/mps/_env.py` | left/right environments and contractions |
| MPS measurement | `external/baselines/yastn/yastn/tn/mps/_measure.py` | expectations and sampling |
| MPS TDVP | `external/baselines/yastn/yastn/tn/mps/_tdvp.py` | time evolution separate from gate-level truncation |
| PEPS state | `external/baselines/yastn/yastn/tn/fpeps/_peps.py` | finite/infinite PEPS container and tensor conventions |
| PEPS gates | `external/baselines/yastn/yastn/tn/fpeps/gates.py` | gate construction and path semantics |
| PEPS evolution | `external/baselines/yastn/yastn/tn/fpeps/_evolution.py` | gate application, optimization, truncation reports |
| Exact-cluster NTU | `external/baselines/yastn/yastn/tn/fpeps/envs/_env_ntu.py` | positive local metric and neighborhood update |
| Boundary-MPS environment | `external/baselines/yastn/yastn/tn/fpeps/envs/_env_boundary_mps.py` | finite contraction approximation |
| CTM environment | `external/baselines/yastn/yastn/tn/fpeps/envs/_env_ctm.py` | CTMRG update and full-environment approximation |
| Bond-metric test | `external/baselines/yastn/tests/peps/test_bond_metric.py` | metric construction and corruption-sensitive checks |
| Truncation-method test | `external/baselines/yastn/tests/peps/test_truncation_methods.py` | SVD/EAT/optimized truncation comparisons |
| MPS truncation test | `external/baselines/yastn/tests/mps/test_truncate.py` | canonical MPS rank and error behavior |

Questions to carry while reading:

- Which reported truncation error is a true cluster norm and which is only an estimator?
- How do EnvNTU, EnvBoundaryMPS, and EnvCTM change the same bond metric?
- How are non-Hermitian or indefinite approximate metrics diagnosed or repaired?

## Quimb

- Clone: `external/baselines/quimb`
- Environment: `ecs-baseline-quimb-peps`
- Frozen input: `main` at `3c89529fe0a3487133a3928201691161e110abdf`
- Scope: Independent Python arbitrary-geometry tensor-network stack; its PEPS simple-update circuit path materialized the frozen d5 complete state.
- Data flow: `graph edges + gate stream -> CircuitPEPSSimpleUpdate -> local gate/SVD updates -> exact open-index contraction -> complete state`

| Read in this order | Path | What to learn |
|---|---|---|
| PEPS circuit entry point | `external/baselines/quimb/quimb/tensor/circuit/peps.py` | CircuitPEPS and CircuitPEPSSimpleUpdate construction |
| Simple-update circuit core | `external/baselines/quimb/quimb/tensor/circuit/simple_update.py` | gate application, gauges, max-bond, and cutoff policy |
| Circuit gate representation | `external/baselines/quimb/quimb/tensor/circuit/gates.py` | raw gate payloads and target-qubit conventions |
| Arbitrary-geometry state | `external/baselines/quimb/quimb/tensor/tensor_arbgeom.py` | graph-local tensor-network state operations |
| Two-dimensional PEPS core | `external/baselines/quimb/quimb/tensor/tensor_2d.py` | finite PEPS geometry, contraction, and boundary methods |
| Tensor primitives | `external/baselines/quimb/quimb/tensor/tensor_core.py` | index ordering, contraction, and dense materialization |
| Decomposition policy | `external/baselines/quimb/quimb/tensor/decomp.py` | SVD backends and rank truncation |
| PEPS circuit tests | `external/baselines/quimb/tests/test_tensor/test_circuit/test_peps.py` | public circuit construction and gate behavior |
| PEPO circuit tests | `external/baselines/quimb/tests/test_tensor/test_circuit/test_pepo.py` | operator-network circuit route |
| 2D TEBD tests | `external/baselines/quimb/tests/test_tensor/test_tn2d/test_tebd.py` | two-dimensional local evolution behavior |

Questions to carry while reading:

- Which error belongs to simple-update bond truncation and which belongs to the final contraction?
- How do gauge_smudge, cutoff, and max_bond interact at a two-site update?
- When can a complete dense vector be materialized without substituting a local or boundary metric?

## Pepsy

- Clone: `external/baselines/pepsy`
- Environment: `ecs-baseline-pepsy`
- Frozen input: `main` at `27cb956ec88a739daece90407833bd3c3f8e1d8f`
- Scope: Quimb-backed Python finite-PEPS toolkit with public gate, simple-update, boundary, BP, sampling, and dense-contraction surfaces; it materialized the frozen d5 complete state.
- Data flow: `product PEPS -> public gate/reduce-split updates -> capped PEPS -> exact open-index contraction -> complete state`

| Read in this order | Path | What to learn |
|---|---|---|
| Public API | `external/baselines/pepsy/src/pepsy/__init__.py` | exported constructors, gate surface, and backend selection |
| Tensor representation | `external/baselines/pepsy/src/pepsy/tensors/core.py` | PEPS tensor ownership, sites, bonds, and index conventions |
| Tensor contraction | `external/baselines/pepsy/src/pepsy/tensors/contractions.py` | dense/open-index contraction mechanics |
| Gate application | `external/baselines/pepsy/src/pepsy/operators/gate_apply.py` | one- and two-site gate update paths |
| Gate construction | `external/baselines/pepsy/src/pepsy/operators/gates.py` | operator and gate conventions |
| PEPS simple update | `external/baselines/pepsy/src/pepsy/optimizers/peps/simple_update.py` | environment-free local truncation |
| PEPS optimizer | `external/baselines/pepsy/src/pepsy/optimizers/peps/optimizer.py` | higher-level update scheduling and controls |
| Boundary metrics | `external/baselines/pepsy/src/pepsy/boundary/metrics.py` | boundary approximation diagnostics |
| Boundary sweeps | `external/baselines/pepsy/src/pepsy/boundary/sweeps.py` | finite boundary contraction workflow |
| PEPS sampling | `external/baselines/pepsy/src/pepsy/sampling/peps.py` | sampling route distinct from a complete vector |
| Gate regression tests | `external/baselines/pepsy/tests/test_gate.py` | routing, local dimensions, bond caps, and PEPO/MPO construction |
| PEPS optimizer tests | `external/baselines/pepsy/tests/test_optimize_peps.py` | target flow, compression, normalization, and diagnostics |

Questions to carry while reading:

- What does reduce-split discard at fixed max_bond when cutoff is zero?
- Which boundary or BP diagnostics are estimators rather than whole-state fidelity?
- How does output-index order propagate from the PEPS sites into to_dense?

## TensorNetworkQuantumSimulator.jl

- Clone: `external/baselines/TensorNetworkQuantumSimulator.jl`
- Environment: `ecs-baseline-tnqs`
- Frozen input: `main` at `b5d4089849de1cc23806aa8325e8db56a55f2e0b`
- Scope: Independent Julia/ITensors arbitrary-graph state simulator with gate application, simple update, truncation, boundary MPS, belief propagation, and sampling; no admitted public d5 complete-state exporter was found.
- Data flow: `graph + TensorNetworkState -> gate sequence -> simple/full update and truncation -> contraction or observable/sampling readout`

| Read in this order | Path | What to learn |
|---|---|---|
| Package surface | `external/baselines/TensorNetworkQuantumSimulator.jl/src/TensorNetworkQuantumSimulator.jl` | module exports and implementation layout |
| State representation | `external/baselines/TensorNetworkQuantumSimulator.jl/src/TensorNetworks/tensornetworkstate.jl` | graph state, physical indices, and constructors |
| Gate definitions | `external/baselines/TensorNetworkQuantumSimulator.jl/src/Apply/gate_definitions.jl` | local gate construction and conventions |
| Gate application | `external/baselines/TensorNetworkQuantumSimulator.jl/src/Apply/apply_gates.jl` | routing and update selection |
| Simple update | `external/baselines/TensorNetworkQuantumSimulator.jl/src/Apply/simple_update.jl` | factorized environments, SVD, and truncation error |
| Truncation | `external/baselines/TensorNetworkQuantumSimulator.jl/src/truncate.jl` | network-level bond truncation controls |
| Contraction | `external/baselines/TensorNetworkQuantumSimulator.jl/src/contract.jl` | network contraction and output behavior |
| Contraction sequencing | `external/baselines/TensorNetworkQuantumSimulator.jl/src/contraction_sequences.jl` | path selection and execution order |
| Boundary-MPS example | `external/baselines/TensorNetworkQuantumSimulator.jl/examples/boundarymps.jl` | finite contraction example |
| 2D dynamics example | `external/baselines/TensorNetworkQuantumSimulator.jl/examples/2dIsing_dynamics.jl` | end-to-end two-dimensional evolution |
| Apply tests | `external/baselines/TensorNetworkQuantumSimulator.jl/test/test_apply.jl` | gate/update behavior |
| Boundary-MPS tests | `external/baselines/TensorNetworkQuantumSimulator.jl/test/test_boundarymps.jl` | boundary contraction checks |

Questions to carry while reading:

- Does a public API preserve every physical index in a declared global basis order?
- Can complete-state export be added without confusing an approximate contraction with fidelity?
- How should the local CHOLMOD ABI mismatch be isolated from upstream algorithm correctness?

## variPEPS Python

- Clone: `external/baselines/variPEPS_Python`
- Environment: `ecs-baseline-varipeps`
- Frozen input: `main` at `0edc81acc634e1465264d53f224101d66dcf04e2`
- Scope: JAX iPEPS, CTMRG/split-CTMRG, expectations, correlations, and variational optimization; not a finite trajectory executor.
- Data flow: `periodic unit cell -> double-layer contractions -> CTMRG projectors/absorption -> converged environment -> observables/gradient`

| Read in this order | Path | What to learn |
|---|---|---|
| PEPS tensor | `external/baselines/variPEPS_Python/varipeps/peps/tensor.py` | site tensor, environment tensors, and local dimensions |
| Periodic unit cell | `external/baselines/variPEPS_Python/varipeps/peps/unitcell.py` | iPEPS topology and translation structure |
| Contraction definitions | `external/baselines/variPEPS_Python/varipeps/contractions/definitions.py` | named contraction networks and index conventions |
| Contraction application | `external/baselines/variPEPS_Python/varipeps/contractions/apply.py` | JAX contraction execution |
| CTMRG projectors | `external/baselines/variPEPS_Python/varipeps/ctmrg/projectors.py` | projector construction and truncation surface |
| CTMRG absorption | `external/baselines/variPEPS_Python/varipeps/ctmrg/absorption.py` | directional environment update |
| CTMRG convergence | `external/baselines/variPEPS_Python/varipeps/ctmrg/routine.py` | iteration, convergence, and custom rules |
| SVD utilities | `external/baselines/variPEPS_Python/varipeps/utils/svd.py` | decomposition backend and custom differentiation |
| Expectation model | `external/baselines/variPEPS_Python/varipeps/expectation/model.py` | observable aggregation boundary |
| One-site expectation | `external/baselines/variPEPS_Python/varipeps/expectation/one_site.py` | smallest observable read path |
| Correlation length | `external/baselines/variPEPS_Python/varipeps/corrlength/corrlength.py` | transfer spectrum diagnostic |
| Square-lattice example | `external/baselines/variPEPS_Python/examples/heisenberg_afm_square.py` | end-to-end construction/optimization/readout |

Questions to carry while reading:

- How do ordinary and split CTMRG control double-layer growth?
- Which convergence tests monitor environment spectra versus physical observables?
- Which pieces can validate a frozen PEPS contraction without importing iPEPS assumptions into a finite patch?

## ITensorMPS.jl

- Clone: `external/baselines/ITensorMPS.jl`
- Environment: `ecs-baseline-itensor`
- Frozen input: `main` at `7ce812c42bfedcb3da1c250fdd5f19cb20394d4d`
- Scope: Independent Julia canonical MPS/MPO implementation with explicit apply, truncate, TDVP, and sweep policies; no PEPS.
- Data flow: `site indices/state -> gate or MPO apply -> orthogonalize/SVD -> cutoff/maxdim truncation -> expectation/sample`

| Read in this order | Path | What to learn |
|---|---|---|
| Core MPS representation | `external/baselines/ITensorMPS.jl/src/mps.jl` | construction, tensors, canonical position, sampling |
| Shared MPS algorithms | `external/baselines/ITensorMPS.jl/src/abstractmps.jl` | orthogonalize, truncate!, gate apply, MPS algebra |
| MPO application | `external/baselines/ITensorMPS.jl/src/mpo.jl` | density-matrix, zip-up, and exact-then-truncate algorithms |
| Default numerical policy | `external/baselines/ITensorMPS.jl/src/defaults.jl` | default max dimension and cutoff |
| Sweep policy | `external/baselines/ITensorMPS.jl/src/sweeps.jl` | per-sweep cutoff/maxdim/mindim/noise |
| TDVP driver | `external/baselines/ITensorMPS.jl/src/solvers/tdvp.jl` | time-evolution entry point |
| Sweep update | `external/baselines/ITensorMPS.jl/src/solvers/sweep_update.jl` | factorization and truncation during solver sweeps |
| MPS tests | `external/baselines/ITensorMPS.jl/test/base/test_mps.jl` | representation and operation invariants |
| Contraction tests | `external/baselines/ITensorMPS.jl/test/base/test_solvers/test_contract.jl` | MPO/MPS application algorithms |
| Gate-evolution example | `external/baselines/ITensorMPS.jl/examples/gate_evolution/quantum_simulator.jl` | circuit-style gate application |

Questions to carry while reading:

- What Spectrum/truncation information is available from each SVD callback?
- How do density-matrix, zip-up, and exact-then-truncate MPO application differ?
- Which canonicalization invariants make discarded Schmidt weight globally interpretable?

## Safe use rules

1. Keep every upstream clone clean; never place adapters, patches, or generated artifacts inside it.
2. Exchange neutral arrays/circuits/manifests across fresh processes; do not co-load JAX, Torch, Aer, and Julia runtimes.
3. Freeze index order, physical dimension, boundary condition, gate convention, precision, and measurement coordinate.
4. Compare no-truncation/full-rank paths before finite-rank paths.
5. Separate state truncation, environment approximation, contraction approximation, sampling error, and record folding.
6. Require an independent deliberate corruption before treating agreement as evidence.
7. A local fidelity, discarded weight, CTM convergence, or bond metric is not by itself a bound on full record TV.

## Regeneration

```bash
python tools/gen_external_tn_code_map.py
python tools/gen_external_tn_code_map.py --check
```

Regenerate after deliberately updating any external baseline commit or changing the curated learning routes.
