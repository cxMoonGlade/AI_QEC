# util — shared small utilities  (placeholder)

Reserved home for small, cross-cutting helper tools needed by future development.
Currently empty.

**Boundary.** Genuine small helpers only — not model logic (→ the capability
modules `calibration` / `understand` / `knobs` / `prediction`), not substrate
(→ `forward` / `mechanisms` / `contexts` / `decoder`), not audits (→ `audit`).
The permanent numerical floor `NUMERICAL_ZERO` lives at `qec_twin.numerics`, not
here.
