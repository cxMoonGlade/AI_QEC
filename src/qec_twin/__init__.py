"""Teacher-learner, finance-structured QEC error-mechanism digital twin.

The model is four capability modules — ``calibration`` (recover), ``understand``,
``knobs`` (manipulate), ``prediction`` — over the exact differentiable forward
substrate (``forward`` / ``forward.exact``), with evaluator-side checks in
``audit``. Binding spec: ``docs/TWIN.md``.
"""
