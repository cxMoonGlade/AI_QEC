run:
  name: d3_r1_mvp03_rank5_global_exact
  output_dir: outputs/scope_static/MVP03
  device: cuda
  dtype: float64

circuit:
  family: surface_code:rotated_memory_x
  distance: 3
  rounds: 1
  noise:
    after_clifford_depolarization: 0.001
    after_reset_flip_probability: 0.001
    before_measure_flip_probability: 0.001
    before_round_data_depolarization: 0.001

graph:
  canonicalize_duplicate_masks: true
  residual_rank: 5

experiment:
  seeds: [0, 1, 2]
  teacher_modes: [exact_orbit, in_family_soft_residual, out_of_family_residual]
  epsilon_breaks: [0.0, 0.1, 0.3]
  shot_budgets: [128, 512, 2048]
  heldout_shots: 4096
  threshold_epsilon: 0.01
  threshold_seed_policy: mean

training:
  models: [local, dmle_qec, hard_orbit, soft_feature_orbit]
  model_options:
    dmle_qec:
      perturb_scale: 0.5
  steps: 200
  lr: 0.05
  aggregate_unique: true
  likelihood_backend: auto
  exact_likelihood_trainable: true
  dem_fault_logit_claim: true
  cptp_gksl_claim: false
